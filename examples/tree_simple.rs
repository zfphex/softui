#![allow(unused)]
use softui::{flext, flext2, ht, ht2, tree::*, tree_simplier::*, tree_widget::*};

fn main() {
    // let mut tree = Tree::new();
    // let root = tree.add_node(Node::default());
    // let rect = rect().wfill().h(20);
    // tree.add_generic_child(root, rect);

    // let h = ht2!(rect().w(10));

    // let flex = flext2!(h);

    let f = flext!(ht!());

    // let h = ht!(ht!());
}
